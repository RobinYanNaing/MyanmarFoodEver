<!-- <p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @meal.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @meal.description %>
</p>

<p>
  <strong>Picture:</strong>

</p>
  <%= image_tag(@meal.picture_url, width: 100) if @meal.picture.present? %>
 
<h3>Comments</h3>
<% @comments.each do |comment| %>
  <div>
    <strong><%= comment.user_name %></strong>
    <br>
    <p><%= comment.body %></p>
    <p><%= link_to 'Delete', comment_path(comment), method: :delete, data: { confirm: 'Are you sure?' } %></p>
  </div>
<% end %>
<%= render partial: 'comments/form', locals: { comment: @comment } %>

<%= link_to 'Edit', edit_meal_path(@meal) %> |
<%= link_to 'Back', meals_path %>
 
 -->

<p id="notice"><%= notice %></p>

<div class="row">
  <div class="col-md-9">
    <%= image_tag(@meal.picture_url, width: '100%') if @meal.picture.present? %>
  </div>

  <div class="col-md-3">
    <p><b>Name: </b><%= @meal.name %></p>
    <p><b>Description: </b><%= @meal.description %></p>
    <p>
      <%= link_to 'Edit', edit_meal_path(@idea) %> |
      <%= link_to 'Destroy', @meal, data: { confirm: 'Are you sure?' }, method: :delete %> |
      <%= link_to 'Back', meals_path %>
    </p>
  </div>
</div> 